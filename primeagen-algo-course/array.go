package leetcodego

// Insertions in array is not a 'creation' or 'growth', it is just an overwrite of a specific piece of memory.

// Deletions in a raw array is not 'removal' it is again an overwriting of a piece of memory

// Big O notation
// Array's are O(1) insert (overwrite), O(1) deletion (overwrite), and O(1) retrieval.
func UseArray() {
}
